<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Success</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container">
        <div class="card">
            <div class="success-container">
                <div class="success-icon"></div>

                <h2>Payment Successful!</h2>

                <div class="status-badge">
                    SUCCESS - Bank Settlement Completed
                </div>

                <div class="transaction-details">
                    <div class="detail-row">
                        <span class="detail-label">Transaction ID</span>
                        <span class="detail-value">
                            <%= transaction.id %>
                        </span>
                    </div>

                    <div class="detail-row">
                        <span class="detail-label">Customer Name</span>
                        <span class="detail-value">
                            <%= transaction.name %>
                        </span>
                    </div>

                    <div class="detail-row">
                        <span class="detail-label">Amount Paid</span>
                        <span class="detail-value amount-highlight">₹<%= transaction.amount.toFixed(2) %></span>
                    </div>

                    <div class="detail-row">
                        <span class="detail-label">Payment Method</span>
                        <span class="detail-value">
                            <span class="payment-method-icon">
                                <% if (transaction.paymentMethod==='UPI' ) { %>📱<% } %>
                                        <% if (transaction.paymentMethod==='Card' ) { %>💳<% } %>
                                                <% if (transaction.paymentMethod==='Wallet' ) { %>👛<% } %>
                                                        <% if (transaction.paymentMethod==='NetBanking' ) { %>🏦<% } %>
                            </span>
                            <%= transaction.paymentMethod %>
                        </span>
                    </div>

                    <div class="detail-row">
                        <span class="detail-label">Settlement Time</span>
                        <span class="detail-value">
                            <%= transaction.settlementTime %>
                        </span>
                    </div>

                    <div class="detail-row">
                        <span class="detail-label">Status</span>
                        <span class="detail-value" style="color: #38ef7d; font-weight: 700;">
                            <%= transaction.status %>
                        </span>
                    </div>

                    <% if (transaction.mobile) { %>
                        <!-- Mobile Recharge Details -->
                        <div style="margin-top: 20px; padding-top: 20px; border-top: 2px solid #e0e0e0;">
                            <h3 style="color: #667eea; margin-bottom: 15px; font-size: 18px;">📱 Mobile Recharge Details
                            </h3>

                            <div class="detail-row">
                                <span class="detail-label">Mobile Number</span>
                                <span class="detail-value" style="font-weight: 700;">
                                    <%= transaction.mobile %>
                                </span>
                            </div>

                            <div class="detail-row">
                                <span class="detail-label">Operator</span>
                                <span class="detail-value">
                                    <%= transaction.operator %>
                                </span>
                            </div>

                            <% if (transaction.rechargeTransactionId) { %>
                                <div class="detail-row">
                                    <span class="detail-label">Recharge ID</span>
                                    <span class="detail-value" style="font-size: 12px;">
                                        <%= transaction.rechargeTransactionId %>
                                    </span>
                                </div>
                                <% } %>

                                    <div class="detail-row">
                                        <span class="detail-label">Recharge Status</span>
                                        <span class="detail-value"
                                            style="color: <%= transaction.rechargeStatus === 'SUCCESS' ? '#38ef7d' : '#ff6b6b' %>; font-weight: 700;">
                                            <%= transaction.rechargeStatus || 'PENDING' %>
                                        </span>
                                    </div>

                                    <% if (transaction.rechargeMessage) { %>
                                        <div
                                            style="margin-top: 15px; padding: 12px; background: #f0f7ff; border-radius: 8px; text-align: center;">
                                            <span style="color: #667eea; font-size: 14px;">
                                                <%= transaction.rechargeMessage %>
                                            </span>
                                        </div>
                                        <% } %>
                        </div>
                        <% } %>
                </div>

                <a href="/" style="text-decoration: none;">
                    <button class="btn btn-secondary">
                        Make Another Payment
                    </button>
                </a>
            </div>
        </div>
    </div>
</body>

</html>