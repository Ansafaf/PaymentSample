<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Successful</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Work Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .success-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            max-width: 600px;
            width: 100%;
            overflow: hidden;
            animation: slideUp 0.5s ease;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .success-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 40px 30px;
            text-align: center;
            color: white;
        }

        .success-icon {
            width: 80px;
            height: 80px;
            background: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            animation: scaleIn 0.5s ease 0.2s both;
        }

        @keyframes scaleIn {
            from {
                transform: scale(0);
            }

            to {
                transform: scale(1);
            }
        }

        .success-icon svg {
            width: 50px;
            height: 50px;
            stroke: #667eea;
            stroke-width: 3;
            stroke-linecap: round;
            stroke-linejoin: round;
            fill: none;
        }

        .checkmark {
            stroke-dasharray: 100;
            stroke-dashoffset: 100;
            animation: drawCheck 0.5s ease 0.5s forwards;
        }

        @keyframes drawCheck {
            to {
                stroke-dashoffset: 0;
            }
        }

        .success-header h1 {
            font-size: 28px;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .success-header p {
            font-size: 16px;
            opacity: 0.9;
        }

        .success-body {
            padding: 30px;
        }

        .transaction-details {
            background: #f8f9ff;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 25px;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid #e0e0e0;
        }

        .detail-row:last-child {
            border-bottom: none;
        }

        .detail-label {
            color: #666;
            font-size: 14px;
            font-weight: 500;
        }

        .detail-value {
            color: #333;
            font-size: 15px;
            font-weight: 600;
            text-align: right;
        }

        .amount-highlight {
            font-size: 24px;
            color: #667eea;
            font-weight: 700;
        }

        .recharge-info {
            background: #e8f5e9;
            border-left: 4px solid #4caf50;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .recharge-info h3 {
            color: #2e7d32;
            font-size: 16px;
            margin-bottom: 8px;
        }

        .recharge-info p {
            color: #1b5e20;
            font-size: 14px;
            margin: 5px 0;
        }

        .action-buttons {
            display: flex;
            gap: 15px;
            margin-top: 25px;
        }

        .btn {
            flex: 1;
            padding: 15px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            text-align: center;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
        }

        .btn-secondary {
            background: white;
            color: #667eea;
            border: 2px solid #667eea;
        }

        .btn-secondary:hover {
            background: #f8f9ff;
        }

        .download-receipt {
            text-align: center;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #e0e0e0;
        }

        .download-link {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .download-link:hover {
            text-decoration: underline;
        }

        @media (max-width: 480px) {
            .success-header h1 {
                font-size: 24px;
            }

            .action-buttons {
                flex-direction: column;
            }

            .detail-row {
                flex-direction: column;
                align-items: flex-start;
                gap: 5px;
            }

            .detail-value {
                text-align: left;
            }
        }
    </style>
</head>

<body>
    <div class="success-container">
        <div class="success-header">
            <div class="success-icon">
                <svg viewBox="0 0 52 52">
                    <path class="checkmark" d="M14 27l7.5 7.5L38 18" />
                </svg>
            </div>
            <h1>Payment Successful!</h1>
            <p>Your transaction has been completed successfully</p>
        </div>

        <div class="success-body">
            <% if (transaction.paymentDetails && transaction.paymentDetails.mobile) { %>
                <!-- Recharge Success Info -->
                <div class="recharge-info">
                    <h3>📱 Mobile Recharge Successful</h3>
                    <p><strong>Mobile Number:</strong>
                        <%= transaction.paymentDetails.mobile %>
                    </p>
                    <p><strong>Operator:</strong>
                        <%= transaction.paymentDetails.operator || 'N/A' %>
                    </p>
                    <% if (transaction.paymentDetails.planDetails) { %>
                        <p><strong>Plan:</strong>
                            <%= transaction.paymentDetails.planDetails.validity %> | <%=
                                    transaction.paymentDetails.planDetails.data %>
                        </p>
                        <% } %>
                </div>
                <% } %>

                    <!-- Transaction Details -->
                    <div class="transaction-details">
                        <div class="detail-row">
                            <span class="detail-label">Transaction ID</span>
                            <span class="detail-value">
                                <%= transaction.transactionId || transaction.orderId %>
                            </span>
                        </div>
                        <div class="detail-row">
                            <span class="detail-label">Order ID</span>
                            <span class="detail-value">
                                <%= transaction.orderId %>
                            </span>
                        </div>
                        <div class="detail-row">
                            <span class="detail-label">Amount Paid</span>
                            <span class="detail-value amount-highlight">₹<%= transaction.amount %></span>
                        </div>
                        <div class="detail-row">
                            <span class="detail-label">Payment Method</span>
                            <span class="detail-value">
                                <%= transaction.paymentMethod %>
                            </span>
                        </div>
                        <div class="detail-row">
                            <span class="detail-label">Status</span>
                            <span class="detail-value" style="color: #4caf50;">✓ <%= transaction.status %></span>
                        </div>
                        <div class="detail-row">
                            <span class="detail-label">Date & Time</span>
                            <span class="detail-value">
                                <%= new Date(transaction.createdAt).toLocaleString('en-IN', { dateStyle: 'medium' ,
                                    timeStyle: 'short' }) %>
                            </span>
                        </div>
                        <% if (transaction.description) { %>
                            <div class="detail-row">
                                <span class="detail-label">Description</span>
                                <span class="detail-value">
                                    <%= transaction.description %>
                                </span>
                            </div>
                            <% } %>
                    </div>

                    <!-- Action Buttons -->
                    <div class="action-buttons">
                        <a href="/" class="btn btn-primary">Back to Home</a>
                        <a href="/payment-gateway?amount=<%= transaction.amount %>" class="btn btn-secondary">Pay
                            Again</a>
                    </div>

                    <!-- Download Receipt -->
                    <div class="download-receipt">
                        <a href="/payment/receipt/<%= transaction.transactionId || transaction.orderId %>"
                            class="download-link">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                stroke-width="2">
                                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                                <polyline points="7 10 12 15 17 10"></polyline>
                                <line x1="12" y1="15" x2="12" y2="3"></line>
                            </svg>
                            Download Receipt
                        </a>
                    </div>
        </div>
    </div>

    <script>
        // Auto-redirect to home after 30 seconds (optional)
        // setTimeout(() => {
        //     window.location.href = '/';
        // }, 30000);

        // Prevent back button
        history.pushState(null, null, location.href);
        window.onpopstate = function () {
            history.go(1);
        };
    </script>
</body>

</html>